import styled from 'styled-components';

import { ModalContent } from '../../../../../components/before/UserListModal/styles';

const StyledModalContent = styled(ModalContent)`
  /* Box */
  height: auto;
`;

export default StyledModalContent;
